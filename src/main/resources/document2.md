ToT和GoT是通过基于搜索的机制来解决LLM推理的挑战的。它们能够生成许多树/图状的推理路径。然而，这两种方法都非常依赖于大量的LLM查询。有时，单个问题的查询数量甚至可以达到数百个。这导致了计算效率的下降。
维护单个不断发展的上下文链，减少了对思维树中冗余查询的需求。它探索了一条多变的推理路径。通过指定某个算法模式让大模型遵循执行，减少ToT、GoT中模型自由发挥可能引入的随机性。 对于算法复杂度明确的问题（如 O(n log n) 的二分查找），正确模拟执行的 AoT 可能比自由探索的 ToT/GoT ​​推理步数更少、路径更优​​。
